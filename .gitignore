# ---- Confidential / internal — never publish ----
CLAUDE.md
PROJECT-NOTES.md
New Local SEO Clients For ProfResults.docx
New Local SEO Clients For ProfResults.md
Website Structure for Perfect SEO.md
SEO SOP/

# Dev/QA artifacts + internal proof-of-work (before/after captures, logs) — never deployed
temporary screenshots/
proof-of-work/
_contactsheet.html

# Internal CEO ops command center (holds client data, owners, tactics — never deployed).
# Source of truth: ops-dashboard/ops-state.json -> build_dashboard.py -> dashboard.html (Artifact).
ops-dashboard/

# Authenticated GBP screenshot tool + its persistent Chrome profile (holds Google
# session cookies — MUST never be committed). Internal ops tooling.
.gbp-chrome/
gbp-shot.mjs

# Internal Leadsnap rank heatmap screenshots (rank data — never deployed to the public site)
*Leadsnap*.JPG
*Leadsnap*.jpg
*Leadsnap*.png

# Internal GBP before/after work + GMB Everywhere scans (proof-of-work + tactics — never deploy)
GBP*.JPG
GBP*.jpg
GBP*.png
GMB*.JPG
GMB*.jpg

# Bulk download archives (duplicate source photos — not for the site)
*.zip

# Source media library — NOT for the repo. The website only uses the curated
# copies in images/. This folder holds full-size originals + large videos
# (.MOV up to ~180MB) that exceed GitHub's 100MB file limit.
Photos & Videos/
*.MOV
*.mov

# OS / tooling
node_modules/
.DS_Store
Thumbs.db
*.log
.vercel
